import { Goal } from "./Goal";

export type UserPreferencesType = {
  height: number;
  weight: number;
  goal: Goal;
  experienceLevel: number;
  workoutsPerWeek: number;
  planLengthPreference: number;
};
